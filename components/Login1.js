import {view, Text, TextInput, Button, Image, TouchableOpacity, 
    StyleSheet, useColorSheme } from "react-native";

    function Login() { 
        return (

        )
    }
const Styles = StyleSheet.create({
    container: {
        flex: 1, 
        //alignItems: 'center',
        justifyContent: 'center',
        padding: 20,
    },

    imageContainer: { alignItems: 'center', marginVertical: 10, },
    
    size: { height: 200, width: 200, borderWidth: 1, borderRadius: 20, },

    title: {
        fontSize: 24,
        fontWeight: '600',
        marginBottom: 16,
        textAlign: 'center',
    },

    label: {
        marginVertical: 8,
    },
});
export default Login;
